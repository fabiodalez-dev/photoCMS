{% extends 'admin/_layout.twig' %}
{% block title %}{{ trans('admin.filter_settings.title') }} — Cimaise{% endblock %}

{% block content %}
<div class="min-h-screen bg-gray-50">
  <!-- Header -->
  <div class="bg-white border-b border-gray-200">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
      <div class="flex items-center justify-between">
        <div>
          <h1 class="text-3xl font-bold text-black">{{ trans('admin.filter_settings.title') }}</h1>
          <p class="text-gray-600 mt-1">{{ trans('admin.filter_settings.description') }}</p>
        </div>
        <div class="flex items-center gap-3">
          <a href="{{ base_path }}/galleries" target="_blank" class="btn-secondary">
            <i class="fas fa-external-link-alt mr-2"></i>
            {{ trans('admin.filter_settings.preview') }}
          </a>
          <form method="post" action="{{ base_path }}/admin/filter-settings/reset" class="inline"
                data-confirm="{{ trans('admin.filter_settings.reset_confirm')|e('html_attr') }}">
            <input type="hidden" name="csrf" value="{{ csrf }}">
            <button type="submit" class="btn-outline-danger">
              <i class="fas fa-undo mr-2"></i>
              {{ trans('admin.filter_settings.reset') }}
            </button>
          </form>
        </div>
      </div>
    </div>
  </div>

  <!-- Main Content -->
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
    <form method="post" action="{{ base_path }}/admin/filter-settings" id="filter-settings-form">
      <input type="hidden" name="csrf" value="{{ csrf }}">
      
      <div class="grid grid-cols-1 xl:grid-cols-3 gap-8">
        
        <!-- Settings Panel -->
        <div class="xl:col-span-2 space-y-6">
          
          <!-- General Settings Card -->
          <div class="card">
            <div class="p-6 border-b border-gray-200">
              <h2 class="text-lg font-semibold text-black flex items-center">
                <i class="fas fa-cogs mr-3 text-gray-600"></i>
                {{ trans('admin.filter_settings.general') }}
              </h2>
              <p class="text-gray-600 text-sm mt-1">{{ trans('admin.filter_settings.general_hint') }}</p>
            </div>
            <div class="p-6 space-y-6">
              <div class="flex items-center justify-between">
                <div>
                  <label class="text-sm font-medium text-black">{{ trans('admin.filter_settings.enable_filter') }}</label>
                  <p class="text-xs text-gray-500 mt-1">{{ trans('admin.filter_settings.enable_filter_hint') }}</p>
                </div>
                <label class="relative inline-flex items-center cursor-pointer">
                  <input type="checkbox" name="enabled" value="1" class="sr-only peer" 
                         {% if settings.general.enabled == '1' %}checked{% endif %}>
                  <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-blue-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-black"></div>
                </label>
              </div>
              
              <div class="flex items-center justify-between">
                <div>
                  <label class="text-sm font-medium text-black">{{ trans('admin.filter_settings.enable_animations') }}</label>
                  <p class="text-xs text-gray-500 mt-1">{{ trans('admin.filter_settings.enable_animations_hint') }}</p>
                </div>
                <label class="relative inline-flex items-center cursor-pointer">
                  <input type="checkbox" name="animation_enabled" value="1" class="sr-only peer" 
                         {% if settings.general.animation_enabled == '1' %}checked{% endif %}>
                  <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-blue-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-black"></div>
                </label>
              </div>
              
              <div>
                <label class="block text-sm font-medium text-black mb-2">{{ trans('admin.filter_settings.animation_duration') }}</label>
                <input type="number" name="animation_duration" step="0.1" min="0.1" max="2.0"
                       value="{{ settings.general.animation_duration }}"
                       class="form-input w-full">
                <p class="text-xs text-gray-500 mt-1">{{ trans('admin.filter_settings.animation_duration_hint') }}</p>
              </div>
            </div>
          </div>

          <!-- Filter Options Card -->
          <div class="card">
            <div class="p-6 border-b border-gray-200">
              <h2 class="text-lg font-semibold text-black flex items-center">
                <i class="fas fa-filter mr-3 text-gray-600"></i>
                {{ trans('admin.filter_settings.available_filters') }}
              </h2>
              <p class="text-gray-600 text-sm mt-1">{{ trans('admin.filter_settings.available_filters_hint') }}</p>
            </div>
            <div class="p-6">
              <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                
                <div class="space-y-4">
                  <div class="flex items-center justify-between">
                    <div class="flex items-center gap-3">
                      <i class="fas fa-folder-open text-gray-600"></i>
                      <div>
                        <span class="text-sm font-medium text-black">{{ trans('admin.filter_settings.categories') }}</span>
                        <p class="text-xs text-gray-500">{{ stats.categories }} {{ trans('admin.filter_settings.available') }}</p>
                      </div>
                    </div>
                    <label class="relative inline-flex items-center cursor-pointer">
                      <input type="checkbox" name="show_categories" value="1" class="sr-only peer" 
                             {% if settings.filters.show_categories == '1' %}checked{% endif %}>
                      <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-blue-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-black"></div>
                    </label>
                  </div>
                  
                  <div class="flex items-center justify-between">
                    <div class="flex items-center gap-3">
                      <i class="fas fa-tags text-gray-600"></i>
                      <div>
                        <span class="text-sm font-medium text-black">{{ trans('admin.filter_settings.tags') }}</span>
                        <p class="text-xs text-gray-500">{{ stats.tags }} {{ trans('admin.filter_settings.available') }}</p>
                      </div>
                    </div>
                    <label class="relative inline-flex items-center cursor-pointer">
                      <input type="checkbox" name="show_tags" value="1" class="sr-only peer" 
                             {% if settings.filters.show_tags == '1' %}checked{% endif %}>
                      <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-blue-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-black"></div>
                    </label>
                  </div>
                  
                  <div class="flex items-center justify-between">
                    <div class="flex items-center gap-3">
                      <i class="fas fa-camera text-gray-600"></i>
                      <div>
                        <span class="text-sm font-medium text-black">{{ trans('admin.filter_settings.cameras') }}</span>
                        <p class="text-xs text-gray-500">{{ stats.cameras }} {{ trans('admin.filter_settings.available') }}</p>
                      </div>
                    </div>
                    <label class="relative inline-flex items-center cursor-pointer">
                      <input type="checkbox" name="show_cameras" value="1" class="sr-only peer" 
                             {% if settings.filters.show_cameras == '1' %}checked{% endif %}>
                      <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-blue-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-black"></div>
                    </label>
                  </div>
                  
                  <div class="flex items-center justify-between">
                    <div class="flex items-center gap-3">
                      <i class="fas fa-eye text-gray-600"></i>
                      <div>
                        <span class="text-sm font-medium text-black">{{ trans('admin.filter_settings.lenses') }}</span>
                        <p class="text-xs text-gray-500">{{ stats.lenses }} {{ trans('admin.filter_settings.available') }}</p>
                      </div>
                    </div>
                    <label class="relative inline-flex items-center cursor-pointer">
                      <input type="checkbox" name="show_lenses" value="1" class="sr-only peer" 
                             {% if settings.filters.show_lenses == '1' %}checked{% endif %}>
                      <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-blue-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-black"></div>
                    </label>
                  </div>
                  
                  <div class="flex items-center justify-between">
                    <div class="flex items-center gap-3">
                      <i class="fas fa-film text-gray-600"></i>
                      <div>
                        <span class="text-sm font-medium text-black">{{ trans('admin.filter_settings.films') }}</span>
                        <p class="text-xs text-gray-500">{{ stats.films }} {{ trans('admin.filter_settings.available') }}</p>
                      </div>
                    </div>
                    <label class="relative inline-flex items-center cursor-pointer">
                      <input type="checkbox" name="show_films" value="1" class="sr-only peer" 
                             {% if settings.filters.show_films == '1' %}checked{% endif %}>
                      <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-blue-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-black"></div>
                    </label>
                  </div>
                </div>
                
                <div class="space-y-4">
                  <div class="flex items-center justify-between">
                    <div class="flex items-center gap-3">
                      <i class="fas fa-flask text-gray-600"></i>
                      <div>
                        <span class="text-sm font-medium text-black">{{ trans('admin.filter_settings.developers') }}</span>
                        <p class="text-xs text-gray-500">{{ trans('admin.filter_settings.for_analog') }}</p>
                      </div>
                    </div>
                    <label class="relative inline-flex items-center cursor-pointer">
                      <input type="checkbox" name="show_developers" value="1" class="sr-only peer" 
                             {% if settings.filters.show_developers == '1' %}checked{% endif %}>
                      <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-blue-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-black"></div>
                    </label>
                  </div>
                  
                  <div class="flex items-center justify-between">
                    <div class="flex items-center gap-3">
                      <i class="fas fa-microscope text-gray-600"></i>
                      <div>
                        <span class="text-sm font-medium text-black">{{ trans('admin.filter_settings.labs') }}</span>
                        <p class="text-xs text-gray-500">{{ trans('admin.filter_settings.dev_labs') }}</p>
                      </div>
                    </div>
                    <label class="relative inline-flex items-center cursor-pointer">
                      <input type="checkbox" name="show_labs" value="1" class="sr-only peer" 
                             {% if settings.filters.show_labs == '1' %}checked{% endif %}>
                      <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-blue-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-black"></div>
                    </label>
                  </div>
                  
                  <div class="flex items-center justify-between">
                    <div class="flex items-center gap-3">
                      <i class="fas fa-map-marker-alt text-gray-600"></i>
                      <div>
                        <span class="text-sm font-medium text-black">{{ trans('admin.filter_settings.locations') }}</span>
                        <p class="text-xs text-gray-500">{{ stats.locations }} {{ trans('admin.filter_settings.available') }}</p>
                      </div>
                    </div>
                    <label class="relative inline-flex items-center cursor-pointer">
                      <input type="checkbox" name="show_locations" value="1" class="sr-only peer" 
                             {% if settings.filters.show_locations == '1' %}checked{% endif %}>
                      <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-blue-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-black"></div>
                    </label>
                  </div>
                  
                  <div class="flex items-center justify-between">
                    <div class="flex items-center gap-3">
                      <i class="fas fa-calendar text-gray-600"></i>
                      <div>
                        <span class="text-sm font-medium text-black">{{ trans('admin.filter_settings.year_filter') }}</span>
                        <p class="text-xs text-gray-500">{{ trans('admin.filter_settings.year_filter_hint') }}</p>
                      </div>
                    </div>
                    <label class="relative inline-flex items-center cursor-pointer">
                      <input type="checkbox" name="show_year" value="1" class="sr-only peer" 
                             {% if settings.filters.show_year == '1' %}checked{% endif %}>
                      <div class="w-11 h-6 bg-gray-200 peer-focus:outline-none peer-focus:ring-4 peer-focus:ring-blue-300 rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-black"></div>
                    </label>
                  </div>
                </div>
              </div>
            </div>
          </div>

          <!-- Grid Layout Card -->
          <div class="card">
            <div class="p-6 border-b border-gray-200">
              <h2 class="text-lg font-semibold text-black flex items-center">
                <i class="fas fa-th mr-3 text-gray-600"></i>
                {{ trans('admin.filter_settings.grid_layout') }}
              </h2>
              <p class="text-gray-600 text-sm mt-1">{{ trans('admin.filter_settings.grid_layout_hint') }}</p>
            </div>
            <div class="p-6 space-y-6">
              <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                <div>
                  <label class="block text-sm font-medium text-black mb-2">{{ trans('admin.filter_settings.desktop_columns') }}</label>
                  <select name="grid_columns_desktop" class="form-input w-full">
                    <option value="2" {% if settings.grid.grid_columns_desktop == '2' %}selected{% endif %}>2 {{ trans('admin.filter_settings.columns') }}</option>
                    <option value="3" {% if settings.grid.grid_columns_desktop == '3' %}selected{% endif %}>3 {{ trans('admin.filter_settings.columns') }}</option>
                    <option value="4" {% if settings.grid.grid_columns_desktop == '4' %}selected{% endif %}>4 {{ trans('admin.filter_settings.columns') }}</option>
                    <option value="5" {% if settings.grid.grid_columns_desktop == '5' %}selected{% endif %}>5 {{ trans('admin.filter_settings.columns') }}</option>
                  </select>
                  <p class="text-xs text-gray-500 mt-1">{{ trans('admin.filter_settings.screen_desktop') }}</p>
                </div>

                <div>
                  <label class="block text-sm font-medium text-black mb-2">{{ trans('admin.filter_settings.tablet_columns') }}</label>
                  <select name="grid_columns_tablet" class="form-input w-full">
                    <option value="1" {% if settings.grid.grid_columns_tablet == '1' %}selected{% endif %}>1 {{ trans('admin.filter_settings.column') }}</option>
                    <option value="2" {% if settings.grid.grid_columns_tablet == '2' %}selected{% endif %}>2 {{ trans('admin.filter_settings.columns') }}</option>
                    <option value="3" {% if settings.grid.grid_columns_tablet == '3' %}selected{% endif %}>3 {{ trans('admin.filter_settings.columns') }}</option>
                  </select>
                  <p class="text-xs text-gray-500 mt-1">{{ trans('admin.filter_settings.screen_tablet') }}</p>
                </div>

                <div>
                  <label class="block text-sm font-medium text-black mb-2">{{ trans('admin.filter_settings.mobile_columns') }}</label>
                  <select name="grid_columns_mobile" class="form-input w-full">
                    <option value="1" {% if settings.grid.grid_columns_mobile == '1' %}selected{% endif %}>1 {{ trans('admin.filter_settings.column') }}</option>
                    <option value="2" {% if settings.grid.grid_columns_mobile == '2' %}selected{% endif %}>2 {{ trans('admin.filter_settings.columns') }}</option>
                  </select>
                  <p class="text-xs text-gray-500 mt-1">{{ trans('admin.filter_settings.screen_mobile') }}</p>
                </div>
              </div>

              <div>
                <label class="block text-sm font-medium text-black mb-2">{{ trans('admin.filter_settings.grid_gap') }}</label>
                <div class="flex items-center gap-4">
                  <label class="flex items-center gap-2">
                    <input type="radio" name="grid_gap" value="small" class="w-4 h-4 text-black border-gray-300 focus:ring-black"
                           {% if settings.grid.grid_gap == 'small' %}checked{% endif %}>
                    <span class="text-sm text-gray-700">{{ trans('admin.filter_settings.gap_small') }}</span>
                  </label>
                  <label class="flex items-center gap-2">
                    <input type="radio" name="grid_gap" value="normal" class="w-4 h-4 text-black border-gray-300 focus:ring-black"
                           {% if settings.grid.grid_gap == 'normal' %}checked{% endif %}>
                    <span class="text-sm text-gray-700">{{ trans('admin.filter_settings.gap_normal') }}</span>
                  </label>
                  <label class="flex items-center gap-2">
                    <input type="radio" name="grid_gap" value="large" class="w-4 h-4 text-black border-gray-300 focus:ring-black"
                           {% if settings.grid.grid_gap == 'large' %}checked{% endif %}>
                    <span class="text-sm text-gray-700">{{ trans('admin.filter_settings.gap_large') }}</span>
                  </label>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Statistics Sidebar -->
        <div class="xl:col-span-1">
          <!-- Statistics Card -->
          <div class="card">
            <div class="p-6 border-b border-gray-200">
              <h3 class="text-lg font-semibold text-black flex items-center">
                <i class="fas fa-chart-bar mr-3 text-gray-600"></i>
                {{ trans('admin.filter_settings.statistics') }}
              </h3>
              <p class="text-gray-600 text-sm mt-1">{{ trans('admin.filter_settings.statistics_hint') }}</p>
            </div>
            <div class="p-6 space-y-4">
              <div class="flex items-center justify-between">
                <span class="text-sm text-gray-600">{{ trans('admin.filter_settings.total_albums') }}</span>
                <span class="text-lg font-semibold text-black">{{ stats.total_albums }}</span>
              </div>
              <div class="flex items-center justify-between">
                <span class="text-sm text-gray-600">{{ trans('admin.filter_settings.categories') }}</span>
                <span class="text-lg font-semibold text-black">{{ stats.categories }}</span>
              </div>
              <div class="flex items-center justify-between">
                <span class="text-sm text-gray-600">{{ trans('admin.filter_settings.tags') }}</span>
                <span class="text-lg font-semibold text-black">{{ stats.tags }}</span>
              </div>
              <div class="flex items-center justify-between">
                <span class="text-sm text-gray-600">{{ trans('admin.filter_settings.cameras') }}</span>
                <span class="text-lg font-semibold text-black">{{ stats.cameras }}</span>
              </div>
              <div class="flex items-center justify-between">
                <span class="text-sm text-gray-600">{{ trans('admin.filter_settings.lenses') }}</span>
                <span class="text-lg font-semibold text-black">{{ stats.lenses }}</span>
              </div>
              <div class="flex items-center justify-between">
                <span class="text-sm text-gray-600">{{ trans('admin.filter_settings.films') }}</span>
                <span class="text-lg font-semibold text-black">{{ stats.films }}</span>
              </div>
              <div class="flex items-center justify-between">
                <span class="text-sm text-gray-600">{{ trans('admin.filter_settings.locations') }}</span>
                <span class="text-lg font-semibold text-black">{{ stats.locations }}</span>
              </div>
            </div>
          </div>

          <!-- Preview Card -->
          <div class="card mt-6">
            <div class="p-6 border-b border-gray-200">
              <h3 class="text-lg font-semibold text-black flex items-center">
                <i class="fas fa-eye mr-3 text-gray-600"></i>
                {{ trans('admin.filter_settings.live_preview') }}
              </h3>
              <p class="text-gray-600 text-sm mt-1">{{ trans('admin.filter_settings.live_preview_hint') }}</p>
            </div>
            <div class="p-6">
              <div id="preview-grid" class="grid gap-4 mb-4">
                <div class="aspect-[4/3] bg-gray-200 rounded-lg animate-pulse"></div>
                <div class="aspect-[4/3] bg-gray-200 rounded-lg animate-pulse"></div>
                <div class="aspect-[4/3] bg-gray-200 rounded-lg animate-pulse"></div>
              </div>
              <p class="text-xs text-gray-500 text-center">{{ trans('admin.filter_settings.preview_update_hint') }}</p>
            </div>
          </div>
        </div>
      </div>
    </form>
  </div>
</div>

<!-- Sticky Footer with Action Buttons -->
<div class="fixed bottom-0 left-0 lg:left-64 right-0 bg-white border-t border-gray-200 shadow-lg z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
    <div class="flex items-center justify-between">
      <div class="flex items-center gap-3">
        <button type="submit" form="filter-settings-form" class="btn-primary">
          <i class="fas fa-save mr-2"></i>
          {{ trans('admin.filter_settings.save_settings') }}
        </button>
        <button type="button" id="preview-settings" class="btn-secondary">
          <i class="fas fa-eye mr-2"></i>
          {{ trans('admin.filter_settings.preview_changes') }}
        </button>
      </div>

      <div class="text-sm text-gray-500">
        <i class="fas fa-info-circle mr-1"></i>
        {{ trans('admin.filter_settings.changes_applied_hint') }}
      </div>
    </div>
  </div>
</div>

<script nonce="{{ csp_nonce() }}">
// Live preview functionality
document.addEventListener('DOMContentLoaded', function() {
    updateGridPreview();
    
    // Update preview when grid settings change
    document.querySelectorAll('select[name^="grid_"], input[name="grid_gap"]').forEach(element => {
        element.addEventListener('change', updateGridPreview);
    });
    
    // Preview button functionality
    document.getElementById('preview-settings').addEventListener('click', function() {
        // Open galleries page in new tab with current settings (for real preview)
        window.open('{{ base_path }}/galleries', '_blank');
    });
});

function updateGridPreview() {
    const desktopCols = document.querySelector('select[name="grid_columns_desktop"]').value;
    const gap = document.querySelector('input[name="grid_gap"]:checked').value;
    
    let gapSize;
    switch(gap) {
        case 'small': gapSize = '0.5rem'; break;
        case 'large': gapSize = '1rem'; break;
        default: gapSize = '0.75rem';
    }
    
    const previewGrid = document.getElementById('preview-grid');
    previewGrid.style.gridTemplateColumns = `repeat(${Math.min(desktopCols, 3)}, 1fr)`;
    previewGrid.style.gap = gapSize;
}
</script>
{% endblock %}
