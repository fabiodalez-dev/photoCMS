{% extends 'admin/_layout.twig' %}
{% block title %}Pages — About{% endblock %}
{% block content %}
  <div class="flex items-center justify-between mb-8">
    <div>
      <h1 class="text-2xl font-bold text-gray-900">Pages</h1>
      <p class="text-gray-600 mt-1">Modifica la pagina <strong>About</strong></p>
    </div>
    <a href="/about" class="btn-secondary" target="_blank"><i class="fas fa-external-link-alt mr-2"></i>Vedi pagina</a>
  </div>

  <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
    <!-- Editor -->
    <div class="lg:col-span-2">
      <form method="post" action="/admin/pages/about" enctype="multipart/form-data" class="space-y-6">
        <input type="hidden" name="csrf" value="{{ csrf }}">

        <div class="card">
          <div class="p-6">
            <h3 class="text-lg font-medium text-gray-900 mb-4 flex items-center">
              <i class="fas fa-heading mr-3 text-gray-600"></i>
              Testi base
            </h3>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
              <div>
                <label class="block text-sm text-gray-700 mb-1">Titolo pagina</label>
                <input type="text" name="about_title" class="form-input" placeholder="About" value="{{ settings['about.title']|default('About') }}">
              </div>
              <div>
                <label class="block text-sm text-gray-700 mb-1">Sottotitolo</label>
                <input type="text" name="about_subtitle" class="form-input" placeholder="Fotografia analogica e digitale, ..." value="{{ settings['about.subtitle']|default('') }}">
              </div>
              <div>
                <label class="block text-sm text-gray-700 mb-1">Permalink</label>
                <div class="flex items-center gap-2">
                  <span class="text-sm text-gray-500 select-none">/</span>
                  <input type="text" name="about_slug" class="form-input" placeholder="about" value="{{ settings['about.slug']|default('about') }}">
                </div>
                <p class="text-xs text-gray-500 mt-1">Solo lettere, numeri e trattini (kebab-case)</p>
              </div>
            </div>
          </div>
        </div>

        <div class="card">
          <div class="p-6">
            <h3 class="text-lg font-medium text-gray-900 mb-4 flex items-center">
              <i class="fas fa-user mr-3 text-gray-600"></i>
              Testo di presentazione
            </h3>
            <textarea name="about_text" rows="14" class="form-input richtext" placeholder="Scrivi una breve bio, il tuo approccio, riconoscimenti, contatti...">{{ settings['about.text'] }}</textarea>
            <p class="text-xs text-gray-500 mt-2">Supporta testo semplice con a capo. Il frontend mantiene uno stile minimale B/N.</p>
          </div>
        </div>

        <div class="card">
          <div class="p-6">
            <h3 class="text-lg font-medium text-gray-900 mb-4 flex items-center">
              <i class="fas fa-quote-left mr-3 text-gray-600"></i>
              Testo introduzione contatti
            </h3>
            <input type="text" name="about_footer_text" class="form-input" placeholder="Per collaborazioni e commissioni, contattami via email o social." value="{{ settings['about.footer_text']|default('') }}">
            <p class="text-xs text-gray-500 mt-2">Breve frase mostrata come introduzione al form contatti</p>
          </div>
        </div>

        <div class="card">
          <div class="p-6">
            <h3 class="text-lg font-medium text-gray-900 mb-4 flex items-center">
              <i class="fas fa-image mr-3 text-gray-600"></i>
              Foto profilo
            </h3>
            <div class="flex items-start gap-6">
              <div class="w-40 h-40 bg-gray-100 flex items-center justify-center overflow-hidden border border-gray-200">
                {% if settings['about.photo_url'] %}
                  <img src="{{ settings['about.photo_url'] }}" alt="About photo" class="object-cover w-full h-full">
                {% else %}
                  <span class="text-gray-400 text-sm">Nessuna foto</span>
                {% endif %}
              </div>
              <div class="flex-1">
                <input type="file" name="about_photo" accept="image/jpeg,image/png" class="block w-full text-sm text-gray-700 file:mr-4 file:py-2 file:px-4 file:rounded file:border-0 file:text-sm file:font-semibold file:bg-black file:text-white hover:file:bg-gray-800" />
                <p class="text-xs text-gray-500 mt-2">Carica JPG o PNG. Verrà generata una versione web ottimizzata (max 1600px).</p>
              </div>
            </div>
          </div>
        </div>

        <div class="card">
          <div class="p-6">
            <h3 class="text-lg font-medium text-gray-900 mb-4 flex items-center">
              <i class="fas fa-hashtag mr-3 text-gray-600"></i>
              Social
            </h3>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
              <div>
                <label class="block text-sm text-gray-700 mb-1">Instagram</label>
                <input type="url" name="social_instagram" class="form-input" placeholder="https://instagram.com/username" value="{{ settings['about.socials'].instagram|default('') }}">
              </div>
              <div>
                <label class="block text-sm text-gray-700 mb-1">X (Twitter)</label>
                <input type="url" name="social_x" class="form-input" placeholder="https://x.com/username" value="{{ settings['about.socials'].x|default('') }}">
              </div>
              <div>
                <label class="block text-sm text-gray-700 mb-1">Facebook</label>
                <input type="url" name="social_facebook" class="form-input" placeholder="https://facebook.com/username" value="{{ settings['about.socials'].facebook|default('') }}">
              </div>
              <div>
                <label class="block text-sm text-gray-700 mb-1">Flickr</label>
                <input type="url" name="social_flickr" class="form-input" placeholder="https://www.flickr.com/people/username" value="{{ settings['about.socials'].flickr|default('') }}">
              </div>
              <div>
                <label class="block text-sm text-gray-700 mb-1">500px</label>
                <input type="url" name="social_500px" class="form-input" placeholder="https://500px.com/p/username" value="{{ settings['about.socials']['500px']|default('') }}">
              </div>
              <div>
                <label class="block text-sm text-gray-700 mb-1">Behance</label>
                <input type="url" name="social_behance" class="form-input" placeholder="https://www.behance.net/username" value="{{ settings['about.socials'].behance|default('') }}">
              </div>
            </div>
          </div>
        </div>

        <div class="card">
          <div class="p-6">
            <h3 class="text-lg font-medium text-gray-900 mb-4 flex items-center">
              <i class="fas fa-envelope mr-3 text-gray-600"></i>
              Contatti
            </h3>
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
              <div>
                <label class="block text-sm text-gray-700 mb-1">Titolo sezione contatti</label>
                <input type="text" name="contact_title" class="form-input" placeholder="Contatti" value="{{ settings['about.contact_title']|default('Contatti') }}">
              </div>
              <div>
                <label class="block text-sm text-gray-700 mb-1">Email destinatario</label>
                <input type="email" name="contact_email" class="form-input" placeholder="you@example.com" value="{{ settings['about.contact_email']|default('') }}">
              </div>
              <div>
                <label class="block text-sm text-gray-700 mb-1">Prefisso oggetto</label>
                <input type="text" name="contact_subject" class="form-input" placeholder="Portfolio" value="{{ settings['about.contact_subject']|default('Portfolio') }}">
              </div>
            </div>
            <p class="text-xs text-gray-500 mt-2">Il form About invierà una email a questo indirizzo usando PHP mail()</p>
          </div>
        </div>

        <div class="flex items-center justify-end gap-3">
          <a href="/admin" class="btn-secondary">Annulla</a>
          <button type="submit" class="btn-primary"><i class="fas fa-save mr-2"></i>Salva</button>
        </div>
      </form>
    </div>

    <!-- Preview note -->
    <div class="lg:col-span-1">
      <div class="card">
        <div class="p-6">
          <h3 class="text-lg font-medium text-gray-900 mb-2">Stile</h3>
          <p class="text-sm text-gray-600">La pagina About usa un layout minimale in bianco e nero, con ampia tipografia e immagine a tutta altezza su desktop.</p>
        </div>
      </div>
    </div>
  </div>
  <script src="https://cdn.jsdelivr.net/npm/tinymce@6.8.3/tinymce.min.js" referrerpolicy="no-referrer"></script>
  <script>
    document.addEventListener('DOMContentLoaded', function(){
      if (window.tinymce) {
        tinymce.init({
          selector: 'textarea.richtext',
          menubar: false,
          statusbar: false,
          branding: false,
          plugins: 'link lists autoresize',
          toolbar: 'undo redo | styles | bold italic | bullist numlist | blockquote | link | removeformat',
          block_formats: 'Paragrafo=p; Sottotitolo=h3; Titolo sezione=h2; Nota=h4',
          default_link_target: '_blank',
          link_default_protocol: 'https',
          rel_list: [{title: 'noopener', value: 'noopener'},{title:'noreferrer', value:'noreferrer'}],
          content_css: false,
          content_style: 'body{font-family:system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,sans-serif; color:#111; line-height:1.7} a{color:#000; text-decoration:underline} blockquote{border-left:3px solid #e5e7eb; margin:1rem 0; padding-left:.75rem; color:#444}',
          valid_elements: 'p,br,strong/b,em/i,ul,ol,li,blockquote,a[href|target|rel],h2,h3,h4,hr',
          height: 420
        });
      }
    });
  </script>
{% endblock %}
