{% extends "frontend/_layout.twig" %}

{% block content %}
<div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-24 text-center" data-page="404">
  <h1 class="text-4xl font-light mb-4">{{ trans('error.404') }}</h1>
  <p class="text-neutral-600 mb-6">{{ trans('error.404_message') }}</p>
  <a href="{{ base_path }}/" class="inline-flex items-center gap-2 px-4 py-2 border border-neutral-300 hover:border-neutral-400">
    <i class="fas fa-home"></i>
    {{ trans('error.go_home') }}
  </a>
</div>
{% endblock %}

